name: "Build and Release"

on:
  workflow_call:
    inputs:
      changelog-entry:
        description: "The name of the CHANGELOG entry to build the release notes from"
        required: true
        type: string
      is-stable:
        description: "Whether we're building for a stable release or nightly"
        required: true
        type: boolean
      release-name:
        description: "Name of the release"
        required: true
        type: string
      release-tag:
        description: "The tag to attach the release to"
        required: true
        type: string

jobs:
  build-neovim:
    name: Build Neovim plugins
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        include:
          - os: ubuntu-latest
            release-artifacts-package: ".#neovim-release-artifacts-linux"
          - os: macos-latest
            release-artifacts-package: ".#neovim-release-artifacts-macos"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.release-tag }}

      - name: Install Nix
        uses: ./.github/actions/setup-nix
        with:
          cachix-auth-token: ${{ secrets.CACHIX_AUTH_TOKEN }}
          ssh-private-keys: |
            ${{ secrets.ABS_PATH_DEPLOY_KEY }}
            ${{ secrets.CAUCHY_DEPLOY_KEY }}
            ${{ secrets.COLLAB_DEPLOY_KEY }}
            ${{ secrets.LAZY_AWAIT_DEPLOY_KEY }}
            ${{ secrets.PANDO_DEPLOY_KEY }}
            ${{ secrets.PUFF_DEPLOY_KEY }}

      - name: Create /RELEASE_TAG
        run: |
          printf "${{ inputs.release-tag }}" > ./RELEASE_TAG
          git add ./RELEASE_TAG

      - name: Build the Neovim release artifacts
        run: nix build ${{ matrix.release-artifacts-package }}

      - name: Upload all artifacts
        uses: actions/upload-artifact@v4
        with:
          name: neovim-${{ matrix.os }}
          path: result/*

  release:
    needs: [build-neovim]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.release-tag }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Get the Changelog entry
        id: changelog-entry
        uses: mindsers/changelog-reader-action@v2
        with:
          version: ${{ inputs.changelog-entry }}
          validation_level: warn

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          name: ${{ inputs.release-name }}
          body: ${{ steps.changelog-entry.outputs.changes }}
          make_latest: ${{ inputs.is-stable && 'true' || 'false' }}
          prerelease: ${{ !inputs.is-stable }}
          tag_name: ${{ inputs.release-tag }}

      - name: Delete all artifacts after creating the release
        uses: geekyeggo/delete-artifact@v5
        with:
          name: "*"
